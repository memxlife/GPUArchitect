[
  {
    "id": "q_compute_problem_size_scaling",
    "name": "Map compute-regime scaling versus problem size",
    "status": "enabled",
    "benchmark": "compute",
    "benchmark_name": "compute_fma_like",
    "type": "scaling_analysis",
    "priority": 0.9,
    "uncertainty": 0.72,
    "readiness": 0.95,
    "estimated_cost": 0.28,
    "rationale": "Current knowledge establishes the inner_iters crossover and weak block-size sensitivity at one compute-heavy setting, but it does not show how runtime scales with total work size n. Measuring runtime across n in both the flat and compute-dominated regimes can reveal launch/overhead effects, throughput saturation, and whether the observed crossover depends on problem size. This is high-value architectural knowledge because it separates fixed launch costs from steady-state compute throughput using only available benchmarks.",
    "parameter_space": {
      "benchmark": "compute_fma_like",
      "n": [
        65536,
        262144,
        1048576,
        4194304,
        16777216,
        33554432,
        67108864
      ],
      "threads_per_block": [
        128,
        256
      ],
      "inner_iters": [
        16,
        64,
        256,
        1024,
        4096
      ],
      "iterations": [
        10,
        20
      ]
    },
    "activation": {
      "when": "accepted claims show inner_iters regime change but no n-scaling characterization",
      "requires": [
        "claim_001",
        "claim_3a6ea275"
      ]
    },
    "completion": {
      "success": "Produce a text summary identifying whether runtime versus n is sublinear at small n due to fixed overhead and approximately linear at larger n, separately for low and high inner_iters, and note any shift in the crossover behavior with problem size.",
      "failure": "If measurements are too noisy to distinguish scaling regimes, report that n-dependence remains unresolved."
    },
    "next_if_resolved": [
      "Test whether compute throughput saturates uniformly across block sizes at the largest n",
      "Compare problem-size scaling of compute and memory benchmarks to isolate compute versus bandwidth limits"
    ]
  },
  {
    "id": "q_compute_iterations_stability",
    "name": "Quantify timing stability and warmup effects",
    "status": "enabled",
    "benchmark": "compute",
    "benchmark_name": "compute_fma_like",
    "type": "stability_analysis",
    "priority": 0.76,
    "uncertainty": 0.81,
    "readiness": 0.98,
    "estimated_cost": 0.16,
    "rationale": "Accepted knowledge currently relies on average times from repeated runs, but does not quantify variance, first-iteration overhead, or the number of repetitions needed for stable estimates. This is unresolved and valuable because launch overhead likely dominates the flat regime, making timing noise and warmup effects especially important near the regime boundary. The benchmark can already vary iterations, so this requires no new tools.",
    "parameter_space": {
      "benchmark": "compute_fma_like",
      "n": [
        16777216
      ],
      "threads_per_block": [
        256
      ],
      "inner_iters": [
        16,
        64,
        256,
        1024,
        4096
      ],
      "iterations": [
        1,
        2,
        5,
        10,
        20,
        50
      ]
    },
    "activation": {
      "when": "runtime regimes are known but confidence in measurement stability is not characterized",
      "requires": [
        "claim_001",
        "claim_002"
      ]
    },
    "completion": {
      "success": "Produce a text summary stating whether average runtime converges with increasing iterations, whether single-iteration runs show warmup overhead, and whether variance differs between the flat-runtime and compute-dominated regimes.",
      "failure": "If repeated measurements do not reveal consistent convergence or variance trends, report timing stability as unresolved."
    },
    "next_if_resolved": [
      "Use the stable iteration count for future boundary refinement and scaling experiments",
      "Revisit crossover localization with reduced timing uncertainty"
    ]
  },
  {
    "id": "q_memory_baseline_characterization",
    "name": "Establish memory benchmark scaling baseline",
    "status": "enabled",
    "benchmark": "memory",
    "benchmark_name": "memory_stream_like",
    "type": "baseline_characterization",
    "priority": 0.93,
    "uncertainty": 0.95,
    "readiness": 0.62,
    "estimated_cost": 0.35,
    "rationale": "All accepted claims concern compute behavior; there is no corresponding memory-side baseline. A basic memory benchmark sweep is high-value because it enables architectural comparison between compute-limited and bandwidth-limited regimes using available benchmark families. The goal is to determine whether memory runtime scales primarily with problem size and how sensitive it is to block size, without requiring profiling tools.",
    "parameter_space": {
      "benchmark": "memory_stream_like",
      "n": [
        65536,
        262144,
        1048576,
        4194304,
        16777216,
        33554432
      ],
      "threads_per_block": [
        64,
        128,
        256,
        512
      ],
      "inner_iters": [
        1,
        2,
        4,
        8
      ],
      "iterations": [
        10,
        20
      ]
    },
    "activation": {
      "when": "memory benchmark family is available but lacks accepted scaling claims",
      "requires": []
    },
    "completion": {
      "success": "Produce a text summary describing whether memory benchmark runtime is primarily size-scaled, whether there is a flat small-n overhead regime, and whether block-size sensitivity is stronger than in compute_fma_like.",
      "failure": "If the memory benchmark does not execute or results are too inconsistent to infer scaling, report the missing baseline explicitly."
    },
    "next_if_resolved": [
      "Directly compare compute and memory scaling laws to classify likely architectural bottlenecks",
      "Refine memory-side block sensitivity in the dominant regime"
    ]
  }
]